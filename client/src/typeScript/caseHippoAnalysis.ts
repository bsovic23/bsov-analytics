export interface CasehippoData {
    module: number,
    fiscalYear: string,
    profession: string,
    member: Boolean,
    enrolled: Boolean,
    complete: Boolean,
    moduleName: string,
    moduleTopic: string;
}